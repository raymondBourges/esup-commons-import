<?xml version="1.0" encoding="UTF-8" ?>

<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-2.0.xsd http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- 
        /properties/ldap/ldap.xml
        The configuration file for domain services.
        This file is referenced by /properties/applicationContext.xml.
    -->

    <bean id="ldapUserService"
        class="org.esupportail.commons.services.ldap.SearchableLdapUserServiceImpl">
        <description>
            This bean provides LDAP facilities to manage ldap users.
        </description>
        <property name="ldapTemplate" ref="ldapTemplate">
            <description>
                The LDAP template used to access the LDAP directory.
            </description>
        </property>
        <property name="dnSubPath" value="${ldap.dnSubPathUsers}">
            <description>The DN sub path.</description>
        </property>
        <property name="idAttribute" value="${ldap.idUserAttribute}">
            <description>
                The LDAP attribute that stores the unique identifier of
                users in the LDAP directory (optional, "uid" by
                default).
            </description>
        </property>
        <property name="attributesAsString"
            value="${ldap.userAttributes}">
            <description>
                The comma-separated list of the LDAP attributes to
                retrieve.
            </description>
        </property>
        <property name="searchAttribute" value="${ldap.searchUserAttribute}">
            <description>The LDAP attribute to search for.</description>
        </property>
        <property name="searchDisplayedAttributesAsString"
            value="${ldap.searchDisplayedUserAttributes}">
            <description>
                The comma-separated list of the LDAP attributes to
                display when searching for users.
            </description>
        </property>
        <property name="testFilter" value="${ldap.testUserFilter}" />
    </bean>

    <bean id="ldapTemplate"
        class="org.springframework.ldap.core.LdapTemplate">
        <description>
            The LDAP template used to acces the LDAP directory. See
            http://ldaptemplate.sourceforge.net.
        </description>
        <property name="contextSource" ref="contextSource">
            <description>
                The data source used by the LDAP template.
            </description>
        </property>
    </bean>

    <bean id="contextSource"
        class="org.springframework.ldap.core.support.LdapContextSource">
        <description>
            This bean describes the physical acces to the LDAP
            directory. In the example below, anonymous and unsecured
            connections will be done to the LDAP directory running on
            server ldap.esup-portail.org on port 389, using the search
            base ou=people,dc=esup-portail,dc=edu, with scope "sub". See
            http://ldaptemplate.sourceforge.net.
        </description>
        <property name="urls">
            <description>The LDAP URL(s) of the directory.</description>
            <list>
                <value>${ldap.url}</value>
                <value>${ldap.alternateUrl}</value>
            </list>
        </property>
        <property name="userDn"
            value="${ldap.userDn}">
            <description>
                The dn used to bind to the LDAP directory.
            </description>
        </property>
        <property name="password" value="${ldap.password}">
            <description>
                The password used to bind to the LDAP directory.
            </description>
        </property>
        <property name="base" value="${ldap.base}">
            <description>The search base (mandatory).</description>
        </property>
        <property name="baseEnvironmentProperties">
            <description>
                The environment properties, for instance to set the
                timeout.
            </description>
            <map>
                <entry key="com.sun.jndi.ldap.connect.timeout"
                    value="${ldap.connectTimeout}" />
            </map>
        </property>
    </bean>

</beans>
